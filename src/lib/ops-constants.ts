/** Critical incident safety override window (seconds). */
export const SAFETY_OVERRIDE_SECONDS = 300;

/** Default / max message history returned on poll. */
export const DEFAULT_MESSAGE_LIMIT = 200;
export const MAX_MESSAGE_LIMIT = 500;

/** Auth rate limits (per IP window). */
export const AUTH_LOGIN_LIMIT = 30;
export const AUTH_REGISTER_LIMIT = 15;
export const AUTH_WINDOW_MS = 60_000;
